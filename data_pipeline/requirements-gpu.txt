# GPU Requirements — Install these on your NVIDIA training machine
# pip install -r requirements-gpu.txt

# Unsloth: QLoRA fine-tuning engine
unsloth[colab-new]>=2024.8

# HuggingFace ecosystem
datasets>=2.19.0
trl>=0.9.0
transformers>=4.43.0

# Evaluation dependencies (also needs data_pipeline/requirements.txt installed)
rich>=13.7.0
json-repair>=0.30.0
pydantic>=2.7.0
torch>=2.1.0
