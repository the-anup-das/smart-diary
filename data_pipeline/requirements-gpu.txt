# Training and local evaluation on an NVIDIA GPU (Linux or WSL2). Install into a fresh venv:
#   pip install -r requirements-gpu.txt
# CUDA: pick the torch wheel that matches your driver first if pip's default does not,
#   e.g. pip install torch --index-url https://download.pytorch.org/whl/cu124
# GGUF export builds llama.cpp on first use and needs cmake, git and a C++ compiler
#   (apt install cmake build-essential git).

torch>=2.4,<2.8
unsloth>=2025.3
transformers>=4.51,<5
trl>=0.20,<0.25
peft>=0.15
bitsandbytes>=0.45
accelerate>=1.0
datasets>=3.0
sentencepiece>=0.2
protobuf>=4.25

# Evaluation with the local weights (also needs data_pipeline/requirements.txt)
rich>=13.7.0
json-repair>=0.30.0
pydantic>=2.7.0
openai>=1.40.0
python-dotenv>=1.0.0
